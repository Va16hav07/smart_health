import 'package:flutter/material.dart';

class WorkoutsPage extends StatelessWidget {
  static const routeName = '/workouts';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Your Workouts Page')));
  }
}
