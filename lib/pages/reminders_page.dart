import 'package:flutter/material.dart';

class RemindersPage extends StatelessWidget {
  static const routeName = '/reminders';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Reminders Page')));
  }
}
